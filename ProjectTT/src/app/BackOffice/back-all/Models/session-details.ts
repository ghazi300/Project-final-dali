export interface SessionDetails {
    id: number;
    userEmail: string;
    taskTitle: string;
   
    endTime: string;  
    pokerCardId: number; // Identifiant de la carte de poker choisie
    // Ajoutez d'autres propriétés si nécessaire
  }